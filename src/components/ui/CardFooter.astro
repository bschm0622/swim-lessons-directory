---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const classes = `flex items-center px-6 [.border-t]:pt-6 ${className || ''}`;
---

<div
  data-slot="card-footer"
  class={classes}
>
  <slot />
</div>
