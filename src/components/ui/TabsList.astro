---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const classes = `bg-muted text-muted-foreground inline-flex h-9 w-fit items-center justify-center rounded-lg p-[3px] ${className || ''}`;
---

<div
  data-slot="tabs-list"
  class={classes}
>
  <slot />
</div>
