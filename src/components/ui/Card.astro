---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const classes = `bg-card text-card-foreground flex flex-col gap-6 rounded-xl border border-border py-6 shadow-sm ${className || ''}`;
---

<div
  data-slot="card"
  class={classes}
>
  <slot />
</div>
